## git-taut

`git-taut` is a command line utility that manages [git-taut] sites in a git repository.

`git-taut` is usually used with `git-remote-taut`.

[git-taut]: https://github.com/rlaf99/git-taut