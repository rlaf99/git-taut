## git-remote-taut

`git-remote-taut` is a command line utility that works with Git as a remote helper that handles [git-taut] site related Git operations.

`git-remote-taut` is usually used with `git-taut`.

[git-taut]: https://github.com/rlaf99/git-taut